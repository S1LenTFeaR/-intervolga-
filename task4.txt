1. SELECT * FROM a, b WHERE a.id=b.a_id; 
2. SELECT * FROM a JOIN b ON a.id=b.a_id;
По результатам работы разницы не будет.
JOIN служит чтобы расширить функционал связывания
таблиц. Но в данном случае таблицы связываются
между собой самым общим образом.